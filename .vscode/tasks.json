{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build rim",
            "type": "shell",
            "command": "cargo build --bin rim",
            "group": "build",
            "problemMatcher": "$rustc"
        },
        {
            "label": "run rim",
            "type": "shell",
            "command": "cargo run --bin rim",
            "group": "build",
            "problemMatcher": "$rustc"
        }
    ]
}